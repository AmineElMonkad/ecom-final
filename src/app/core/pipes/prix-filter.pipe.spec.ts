import { PrixFilterPipe } from './prix-filter.pipe';

describe('PrixFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PrixFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
